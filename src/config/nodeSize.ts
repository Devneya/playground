export const DEFAULT_MOA_PROMPT_NODE_HEIGHT = 64;
export const DEFAULT_CONTENT_NODE_HEADER_HEIGHT = 120;
export const DEFAULT_CONTAINER_NODE_HEADER_HEIGHT = 52;

export type NodeSize = {
  width: number;
  height: number;
};

export const DEFAULT_NODE_SIZE: NodeSize = {
  width: 200,
  height: 200,
};

export const DEFAULT_CONTENT_NODE_SIZE: NodeSize = {
  width: 600,
  height: 350,
};

export const DEFAULT_IMAGE_CONTENT_NODE_SIZE: NodeSize = {
  width: 600,
  height: 650,
};

export const DEFAULT_AUDIO_CONTENT_NODE_SIZE: NodeSize = {
  width: 600,
  height: 144,
};

export const DEFAULT_PROMPT_NODE_SIZE: NodeSize = {
  width: 600,
  height: 250,
};

export const DEFAULT_CONTAINER_NODE_SIZE: NodeSize = {
  width: 520,
  height: 124,
};